# Database
DATABASE_URL=postgres://user:password@localhost:5432/daget_dev

# Discord OAuth
DISCORD_CLIENT_ID=your-discord-client-id
DISCORD_CLIENT_SECRET=your-discord-client-secret
# Set to 1 to open Discord login in a popup, 0 for full-page redirect
NEXT_PUBLIC_DISCORD_AUTH_POP_UP=0

# Discord Bot (required for raffle+Discord features)
DISCORD_BOT_TOKEN=your-bot-token
# Ed25519 public key from Developer Portal — exactly 64 hex characters
DISCORD_PUBLIC_KEY=your-64-char-hex-public-key

# Session JWT signing secret — minimum 32 characters (generate with: openssl rand -hex 32)
SESSION_SECRET=your-64-char-hex-string

# Wallet Encryption (32-byte hex key)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
WALLET_ENC_KEY_V1=your-64-char-hex-string
# WALLET_ENC_KEY_V2= (optional, for key rotation)

# Solana
SOLANA_CLUSTER=devnet
# SOLANA_RPC_URL= (optional custom RPC, defaults to public endpoint)

# Redis (rate limiting)
REDIS_URL=redis://localhost:6379

# Processor health
PROCESSOR_HEALTH_TOKEN=your-internal-token

# Sentry (optional in dev)
# SENTRY_DSN=

# App (REQUIRED — used as the trusted origin for OAuth redirects; no fallback)
NEXT_PUBLIC_APP_URL=http://localhost:3000
